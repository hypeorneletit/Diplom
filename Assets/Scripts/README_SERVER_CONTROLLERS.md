# Система управления серверами - Документация

## 📋 Обзор

Система управления серверами позволяет взаимодействовать с четырьмя серверами в VR-среде без использования традиционных кнопок. Интерфейс активируется автоматически при приближении к серверу или наведении на него.

## 🎯 Основные возможности

- **Автоматическая активация интерфейса** при приближении к серверу (proximity detection)
- **Визуальная обратная связь** при наведении на сервер
- **Отображение информации о сервере** в реальном времени:
  - Температура сервера
  - Нагрузка CPU
  - Статус (Норма/Предупреждение/Критично)
  - Время работы
- **Управление сервером**:
  - Перезагрузка сервера (при нажатии на сервер)
  - Мониторинг состояния в реальном времени

## 📁 Структура файлов

- `ServerController.cs` - Контроллер взаимодействия с сервером
- `ServerControllerWindow.cs` - Окно интерфейса для отображения информации о сервере

## 🚀 Быстрая настройка

### Шаг 1: Подготовка префаба сервера

1. Откройте префаб сервера (например, `servers-1`, `servers-2`, `servers-3`, `servers-3A`)
2. Убедитесь, что у сервера есть:
   - Mesh Renderer компонент
   - Collider (BoxCollider или MeshCollider)

### Шаг 2: Создание UI для окна сервера

1. **Создайте Canvas для окна:**
   - В иерархии префаба сервера создайте пустой GameObject
   - Назовите его `ServerWindow`
   - Добавьте компонент `Canvas`
   - Настройте Canvas:
     - Render Mode: `World Space`
     - Scale: `0.001` (или используйте Canvas Scaler)

2. **Создайте панель окна:**
   - Под `ServerWindow` создайте `Image` GameObject
   - Назовите его `WindowPanel`
   - Настройте:
     - Color: темно-серый с прозрачностью (например, R:20, G:20, B:20, A:230)
     - Anchor: Stretch-Stretch (заполняет весь Canvas)

3. **Создайте текстовые элементы:**
   - **ServerNameText** (TextMeshProUGUI):
     - Размер шрифта: 30
     - Выравнивание: Top Center
     - Anchor: Top (0.85 - 1.0)
   
   - **ServerStatusText** (TextMeshProUGUI):
     - Размер шрифта: 26
     - Выравнивание: Top Left
     - Anchor: Top (0.7 - 0.85)
   
   - **ServerDetailsText** (TextMeshProUGUI):
     - Размер шрифта: 24
     - Выравнивание: Top Left
     - Word Wrapping: включен
     - Anchor: Middle (0.25 - 0.7)
   
   - **ActionHintText** (TextMeshProUGUI):
     - Размер шрифта: 20
     - Выравнивание: Bottom Left
     - Anchor: Bottom (0.0 - 0.25)

### Шаг 3: Добавление компонентов на сервер

1. **На основном объекте сервера** (например, `servers-1`):
   - Добавьте компонент `ServerController`
   - Настройте параметры:
     - `Server Index`: 0 (для первого сервера), 1 (для второго), и т.д.
     - `Server Display Name`: "Сервер 1", "Сервер 2", и т.д.
     - `Interaction Distance`: 2.0 (расстояние активации в метрах)
     - `Use Proximity Detection`: ✓ (включено)
     - `Use Hover Detection`: ✓ (включено)

2. **На объекте ServerWindow:**
   - Добавьте компонент `ServerControllerWindow`
   - Настройте ссылки:
     - `Window Canvas`: ссылка на Canvas
     - `Server Name Text`: ссылка на ServerNameText
     - `Server Status Text`: ссылка на ServerStatusText
     - `Server Details Text`: ссылка на ServerDetailsText
     - `Action Hint Text`: ссылка на ActionHintText
     - `Window Panel`: ссылка на WindowPanel
   - Настройте параметры окна:
     - `Window Distance`: 0.8 (расстояние от игрока)
     - `Window Size`: X: 0.55, Y: 0.75
     - `Window Height`: 0.1
     - `Font Size`: 24
   - Настройте параметры сервера:
     - `Server Index`: должен совпадать с ServerController
     - `Server Display Name`: должен совпадать с ServerController

3. **Настройка визуальной обратной связи (опционально):**
   - В `ServerController` настройте:
     - `Server Renderers`: перетащите все Renderer компоненты сервера
     - `Hover Material`: материал для наведения (опционально)
     - `Normal Material`: обычный материал сервера
     - `Active Material`: материал когда окно открыто (опционально)

### Шаг 4: Настройка XR Interaction

1. **Проверьте наличие XR Interaction Manager в сцене:**
   - Если его нет, создайте пустой GameObject
   - Добавьте компонент `XR Interaction Manager`

2. **Проверьте настройки XR Interactable:**
   - `ServerController` автоматически создаст `XRSimpleInteractable` если его нет
   - Убедитесь, что:
     - `Filter Interaction By Distance`: включено
     - `Max Interaction Distance`: 2.0 (или ваше значение)

## 🎮 Использование

### Для игрока:

1. **Приближение к серверу:**
   - Подойдите к серверу на расстояние менее 2 метров
   - Интерфейс автоматически откроется перед вами

2. **Наведение на сервер:**
   - Наведите контроллер/руку на сервер
   - Интерфейс откроется автоматически

3. **Просмотр информации:**
   - В интерфейсе отображается:
     - Название сервера
     - Текущий статус
     - Температура и нагрузка CPU
     - Подсказки по действиям

4. **Управление сервером:**
   - Нажмите на сервер (select action) для перезагрузки
   - Процесс перезагрузки отображается в реальном времени

5. **Закрытие интерфейса:**
   - Отойдите от сервера на расстояние более 2 метров
   - Интерфейс закроется автоматически

## ⚙️ Расширенные настройки

### Настройка расстояния взаимодействия

В `ServerController`:
- `Interaction Distance`: измените значение для изменения дистанции активации
- Рекомендуемые значения: 1.5 - 3.0 метра

### Настройка визуальной обратной связи

1. Создайте материалы для разных состояний:
   - Нормальный материал (обычное состояние)
   - Material с эмиссией для hover состояния
   - Material с другим цветом для активного состояния

2. Назначьте материалы в `ServerController`:
   - `Normal Material`: обычное состояние
   - `Hover Material`: при наведении
   - `Active Material`: когда окно открыто

### Кастомизация интерфейса

В `ServerControllerWindow` можно настроить:
- Размер окна (`Window Size`)
- Расстояние от игрока (`Window Distance`)
- Размер шрифта (`Font Size`)
- Высоту окна (`Window Height`)

## 🔧 Решение проблем

### Интерфейс не открывается

1. Проверьте, что `MonitoringDataService` существует в сцене
2. Убедитесь, что `Server Index` правильно настроен (0-3)
3. Проверьте наличие `XR Interaction Manager` в сцене
4. Убедитесь, что у сервера есть Collider

### Интерфейс открывается слишком далеко/близко

1. Измените `Interaction Distance` в `ServerController`
2. Измените `Window Distance` в `ServerControllerWindow`

### Интерфейс не закрывается

1. Проверьте настройки `Use Proximity Detection` и `Use Hover Detection`
2. Убедитесь, что игрок действительно отходит от сервера

### Визуальная обратная связь не работает

1. Убедитесь, что `Server Renderers` правильно назначены
2. Проверьте, что материалы существуют и назначены

## 📝 Пример настройки для 4 серверов

### Сервер 1 (servers-1):
- `Server Index`: 0
- `Server Display Name`: "Сервер 1"

### Сервер 2 (servers-2):
- `Server Index`: 1
- `Server Display Name`: "Сервер 2"

### Сервер 3 (servers-3):
- `Server Index`: 2
- `Server Display Name`: "Сервер 3"

### Сервер 4 (servers-3A или servers-4):
- `Server Index`: 3
- `Server Display Name`: "Сервер 4"

## 🎨 Рекомендации по дизайну

1. **Цветовая схема:**
   - Норма: зеленый (#00FF00)
   - Предупреждение: желтый (#FFFF00)
   - Критично: красный (#FF0000)

2. **Размеры интерфейса:**
   - Рекомендуемый размер: 0.55 x 0.75 метра
   - Расстояние от игрока: 0.8 метра

3. **Шрифты:**
   - Используйте TextMeshPro для лучшей читаемости в VR
   - Рекомендуемый размер: 24-30 для заголовков, 20-24 для текста

## 📚 Дополнительная информация

Система интегрирована с `MonitoringDataService` для получения данных о серверах в реальном времени. Убедитесь, что сервис инициализирован в сцене перед использованием системы управления серверами.
