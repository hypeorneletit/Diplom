# Инструкция по настройке VR Energy Control Room System

## ⚡ БЫСТРАЯ НАСТРОЙКА (5 минут)

### 1. Инициализация сервисов (ОБЯЗАТЕЛЬНО!)

**Создайте объекты сервисов в сцене:**

1. Создайте пустой GameObject (GameObject > Create Empty)
2. Назовите его **"Services"**
3. Добавьте компонент **`ServiceInitializer`** на этот объект
   - Он автоматически создаст все необходимые сервисы

**ИЛИ создайте вручную:**
- "MonitoringDataService" с компонентом `MonitoringDataService`
- "EventLogService" с компонентом `EventLogService`
- "AlarmService" с компонентом `AlarmService`

**Важно:** Сервисы должны быть в сцене ДО запуска игры!

---

### 2. Настройка двери (ИСПРАВЛЕНО!)

**Ваша дверь уже имеет `Pivot_Closed` и `Pivot_Open` - код использует их автоматически!**

1. Выберите объект **"Door"** (верхний уровень в иерархии)
2. **Убедитесь, что у двери есть ТОЛЬКО ОДИН XR Interactable компонент:**
   - Либо `XRSimpleInteractable`
   - Либо `XRGrabInteractable`
   - **НЕ используйте оба одновременно!**
3. Добавьте компонент **`DoorController`**
4. **Код автоматически найдет `Pivot_Closed` и использует его!**
5. Настройте параметры (опционально):
   - `Open Angle` - угол открытия (90° по умолчанию)
   - `Open Speed` - скорость открытия (2 = нормальная)
   - `Open Inward` - открывается внутрь (true) или наружу (false)

**Если дверь все еще крутится неправильно:**
- Убедитесь, что `Pivot_Closed` находится на краю двери (где петли)
- Проверьте, что `Door Model` указывает на правильный объект (дочерний "Door")

---

### 3. Настройка мониторов (ТОЛЬКО ОКНО)

**Система окон - окно открывается перед игроком при нажатии на монитор**

1. Выберите объект монитора (Workstation prefab)
2. Добавьте компонент **`WorkstationMonitorController`**
3. **Создайте окно монитора:**
   - Создайте пустой GameObject как **дочерний объект монитора**
   - Назовите его **"MonitorWindow"**
   - Добавьте компонент **`MonitorWindow`**
4. **Создайте Canvas для окна:**
   - Создайте Canvas (GameObject > UI > Canvas) как **дочерний объект MonitorWindow**
   - Установите Render Mode: **World Space**
   - В Canvas создайте:
     - **Panel** для UI (это будет `windowPanel`)
     - **TextMeshPro - Text** для температуры серверной (`serverRoomTempText`)
     - **TextMeshPro - Text** для списка серверов (`serverListText`)
     - **TextMeshPro - Text** для лога событий (`eventLogText`)
     - **Button** для закрытия окна (добавьте `XRSimpleInteractable` на кнопку)
5. Добавьте **`XRSimpleInteractable`** на объект монитора (для взаимодействия)
6. **В инспекторе `WorkstationMonitorController`:**
   - Перетащите MonitorWindow в поле **`Monitor Window`**
   - (Опционально) Укажите Renderer монитора для визуальной обратной связи
7. **В инспекторе `MonitorWindow`:**
   - Перетащите Canvas в поле **`Window Canvas`**
   - Перетащите Panel в поле **`Window Panel`**
   - Перетащите все TextMeshPro элементы в соответствующие поля
   - Перетащите кнопку закрытия в поле **`Close Button`**
   - Настройте размер окна (`Window Size`) - по умолчанию 0.6x0.8 метра

**Canvas автоматически настроится на правильный размер и не будет "летать в воздухе"!**

---

### 4. Настройка главного экрана (Main Dashboard)

1. Выберите объект главного экрана
2. Добавьте компонент **`MainDashboardController`**
3. **Создайте UI структуру:**
   - Canvas (World Space) для экрана
   - TextMeshPro для общего статуса (`overallStatusText`)
   - TextMeshPro для лога событий (`eventLogText`)
   - RectTransform для контейнера графика температуры (`temperatureGraphContainer`)
   - 4 UI Slider для баров серверов (`serverBars`)
   - 4 Image (Fill) для заполнения баров (`serverBarFills`)
   - 4 TextMeshPro для меток баров (`serverBarLabels`)
   - Image для alarm overlay (опционально)
4. **В инспекторе `MainDashboardController`:**
   - Перетащите все UI элементы в соответствующие поля
   - Настройте размеры графика (`Graph Width`, `Graph Height`)

**Если экран ничего не показывает:**
- Убедитесь, что `ServiceInitializer` есть в сцене
- Проверьте консоль на ошибки
- Убедитесь, что все ссылки заполнены

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Дверь крутится неправильно
- ✅ Код автоматически использует `Pivot_Closed` из вашего prefab
- Если все еще проблема: проверьте, что `Pivot_Closed` находится на краю двери
- Убедитесь, что у двери только один XR Interactable компонент

### Canvas "летает в воздухе"
- ✅ `MonitorWindow` автоматически настраивает размер и позицию Canvas
- Canvas должен быть дочерним объектом MonitorWindow
- Размер окна настраивается через `Window Size` в `MonitorWindow`

### Мониторы не показывают данные
- Проверьте, что сервисы созданы (`ServiceInitializer` в сцене)
- Проверьте, что все ссылки в `MonitorWindow` заполнены
- Проверьте консоль на ошибки

### Главный экран не работает
- Убедитесь, что `ServiceInitializer` есть в сцене
- Проверьте, что все ссылки заполнены
- Проверьте консоль на ошибки

---

## ✅ ЧЕКЛИСТ НАСТРОЙКИ

- [ ] Создан объект "Services" с `ServiceInitializer`
- [ ] На дверь добавлен `DoorController` и один XR Interactable
- [ ] На мониторы добавлен `WorkstationMonitorController`
- [ ] Создан MonitorWindow с Canvas и UI элементами
- [ ] Все ссылки в инспекторах заполнены
- [ ] На главный экран добавлен `MainDashboardController`
- [ ] Все UI элементы созданы и привязаны

---

## 📝 ПРИМЕЧАНИЯ

- Все Canvas должны быть в режиме **World Space**
- Размеры UI настраиваются автоматически в `MonitorWindow`
- Дверь использует существующие pivot точки из prefab
- Окна мониторов открываются перед игроком при нажатии