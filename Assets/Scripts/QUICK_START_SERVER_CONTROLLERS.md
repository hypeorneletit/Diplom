# 🚀 Быстрый старт - Система управления серверами

## Что было создано

✅ **ServerController.cs** - Контроллер взаимодействия с сервером  
✅ **ServerControllerWindow.cs** - Окно интерфейса для отображения информации  
✅ **README_SERVER_CONTROLLERS.md** - Полная документация

## Как это работает

1. **Без кнопок!** Интерфейс открывается автоматически при приближении к серверу
2. **Proximity Detection** - интерфейс активируется когда вы подходите близко
3. **Hover Detection** - интерфейс активируется при наведении контроллера
4. **Визуальная обратная связь** - сервер подсвечивается при взаимодействии

## Минимальная настройка (5 минут)

### Для каждого из 4 серверов:

1. **Откройте префаб сервера** (servers-1, servers-2, servers-3, servers-3A)

2. **Создайте UI структуру:**
   ```
   servers-X
   └── ServerWindow (Canvas)
       └── WindowPanel (Image - темный фон)
       └── ServerNameText (TextMeshProUGUI)
       └── ServerStatusText (TextMeshProUGUI)
       └── ServerDetailsText (TextMeshProUGUI)
       └── ActionHintText (TextMeshProUGUI)
   ```

3. **На основном объекте сервера** добавьте:
   - Компонент `ServerController`
   - Установите `Server Index`: 0, 1, 2, или 3 (для каждого сервера свой)
   - Установите `Server Display Name`: "Сервер 1", "Сервер 2", и т.д.

4. **На объекте ServerWindow** добавьте:
   - Компонент `ServerControllerWindow`
   - Перетащите все ссылки (Canvas, тексты, панель)
   - Установите `Server Index` такой же как в ServerController

5. **Готово!** Сохраните префаб и протестируйте в сцене

## Важные моменты

- ✅ Убедитесь что `MonitoringDataService` есть в сцене
- ✅ Убедитесь что `XR Interaction Manager` есть в сцене  
- ✅ У каждого сервера должен быть Collider
- ✅ Server Index должен быть уникальным для каждого сервера (0-3)

## Функционал

- 📊 **Мониторинг**: Температура, CPU, статус в реальном времени
- 🔄 **Перезагрузка**: Нажмите на сервер для перезагрузки
- 🎨 **Визуальная обратная связь**: Сервер подсвечивается при взаимодействии
- 🚪 **Автоматическое закрытие**: Интерфейс закрывается при отдалении

## Пример использования

1. Подойдите к серверу на расстояние ~2 метра
2. Интерфейс автоматически появится перед вами
3. Смотрите информацию о сервере
4. Нажмите на сервер для перезагрузки
5. Отойдите - интерфейс закроется автоматически

## Нужна помощь?

Смотрите полную документацию в `README_SERVER_CONTROLLERS.md`
